/// Reserved for Explorer Radio UI widgets.
///
/// Intentionally empty for now — this phase builds ONLY the backend decision
/// engine (models, repositories, services, controllers). The player UI (now
/// playing, queue, chapters, controls) will live here and will watch
/// `radioEngineControllerProvider` to render the engine's [PlaybackState].
library;
